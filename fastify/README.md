Simple app to test working with fastify.

Create a user by hitting `POST /user` with `email` and `password`, then authenticate as that user using basic auth and hit `GET /user` (the latter doesn't currently work).